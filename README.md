# ChainSpot Staging

Staging deployment target for reviewed ChainSpot builds awaiting manual browser acceptance.

Production source lives in `samuelpmahan/ChainSpot`. This repository should contain staging/deployment infrastructure only; it is not a parallel application-development branch.
